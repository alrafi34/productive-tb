export interface NATOOptions {
  convertNumbers: boolean;
  ignorePunctuation: boolean;
  letterByLetter: boolean;
  realtimeConvert: boolean;
}

export type ViewMode = 'standard' | 'letter-by-letter';
