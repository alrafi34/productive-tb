export interface LeetspeakOptions {
  intensity: 'light' | 'standard' | 'hardcore';
  randomMode: boolean;
  preserveSpaces: boolean;
  realtimeConvert: boolean;
}

export type PresetType = 'gamer' | 'hacker' | 'meme' | 'custom';
